services:
  - type: web
    name: pool-app-demo
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python pool_app_verified/run_pool.py --host 0.0.0.0 --port $PORT --no-browser
